//TODO - figure this out better.  I don't like that I have to name every instance
/*
var coll_obj = collision_circle(mouse_x,mouse_y,2,all,0,1); //detect collision at mouse_x and mouse_y position

if (coll_obj != noone) //if colliding with any obj
{
    //var str_obj_name = object_get_name(coll_obj.object_index); //gets the object name that is colliding at mouse_x, mouse_y position_
    var str_obj_name = coll_obj.name || ""; //access the object colliding at mouse_x,mouse_y and get instance 'name' and store it to str_obj_name.    
    //draw_rectangle_colour(coll_obj.x, coll_obj.y - 35, coll_obj.sprite_width, coll_obj.y - 10, c_white, c_white, c_white, c_white, false); 
    draw_set_font(gamefont);
    draw_set_halign(fa_center);
    //var c = make_colour_rgb(0, 244, 44);
    draw_set_colour(c_black);
    draw_text(coll_obj.x + (coll_obj.sprite_width / 2),coll_obj.y - 25,string(str_obj_name)); //draw the object's name at the mouse_x, mouse_y position

}
*/

